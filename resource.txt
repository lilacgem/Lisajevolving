<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="AuEvoAujouleAuEvoAujoulEvolvE"charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>AuEvoAujoulEvol</title>
    <link rel="icon" type="image/png" href="favicon.png">
        "LisajEvo", "lisajevolving", "evolvinglisaj", "Lisajthaqueen", "Lisaj", "Aujoule",  "AuEvo", "EvoRoot", "EvolvingRoots", "Louisiana",  "Auevolving_Tongue", "Soul_Speaks", "Au_Evolving_Tongue", "SoulSpeaks", "Au","Evolving","Tongue", "Latin", "Auevolving", "lisajEVO", "lisajevolving", "evolvinglisaj", "Lisajthaqueen", "Lisaj", "Aujoule", "AuEvo", "EvoRoot", "EvolvingRoots", "Louisiana",  "philosophy", "numerology", "369_Method", "Ritual3", "Ritual6", "Ritual9", "Sequence", "Master", "Sacred", "Sacred_Geometry", "Holy_Trinity", "Manifestation", "Seed", "Spirit", "Soul", "Soul_Speaks", "AuEvolving_Tongue", "Latin", "Final_Seal", "Visualizer", "Ambient", "High_Contrast", "Symmetry", "Organic_Texture", "Original_Song", "Music", "The_Vault", "Archive_2026", "Gold_Standard", "Library", "Esoteric", "Knowledge", "evolution", "EvoSpace", "SpaceEvo", "Evo2Evo", "Surge", "Frequency", "Manifest"
    >
        "LisajEvo", "lisajevolving", "evolvinglisaj", "Lisajthaqueen", "Lisaj", "Aujoule",  "AuEvo", "EvoRoot", "EvolvingRoots", "Louisiana",  "Auevolving_Tongue", "Soul_Speaks", "Au_Evolving_Tongue", "SoulSpeaks", "Au","Evolving","Tongue", "Latin", "Auevolving", "lisajEVO", "lisajevolving", "evolvinglisaj", "Lisajthaqueen", "Lisaj", "Aujoule", "AuEvo", "EvoRoot", "EvolvingRoots", "Louisiana",  "philosophy", "numerology", "369_Method", "Ritual3", "Ritual6", "Ritual9", "Sequence", "Master", "Sacred", "Sacred_Geometry", "Holy_Trinity", "Manifestation", "Seed", "Spirit", "Soul", "Soul_Speaks", "AuEvolving_Tongue", "Latin", "Final_Seal", "Visualizer", "Ambient", "High_Contrast", "Symmetry", "Organic_Texture", "Original_Song", "Music", "The_Vault", "Archive_2026", "Gold_Standard", "Library", "Esoteric", "Knowledge", "evolution", "EvoSpace", "SpaceEvo", "Evo2Evo", "Surge", "Frequency", "Manifest"
   "LisajEvo", "lisajevolving", "evolvinglisaj", "Lisajthaqueen", "Lisaj", "Aujoule",  "AuEvo", "EvoRoot", "EvolvingRoots", "Louisiana",  "Auevolving_Tongue", "Soul_Speaks", "Au_Evolving_Tongue", "SoulSpeaks", "Au","Evolving","Tongue", "Latin", "Auevolving", "lisajEVO", "lisajevolving", "evolvinglisaj", "Lisajthaqueen", "Lisaj", "Aujoule", "AuEvo", "EvoRoot", "EvolvingRoots", "Louisiana",  "philosophy", "numerology", "369_Method", "Ritual3", "Ritual6", "Ritual9", "Sequence", "Master", "Sacred", "Sacred_Geometry", "Holy_Trinity", "Manifestation", "Seed", "Spirit", "Soul", "Soul_Speaks", "AuEvolving_Tongue", "Latin", "Final_Seal", "Visualizer", "Ambient", "High_Contrast", "Symmetry", "Organic_Texture", "Original_Song", "Music", "The_Vault", "Archive_2026", "Gold_Standard", "Library", "Esoteric", "Knowledge", "evolution", "EvoSpace", "SpaceEvo", "Evo2Evo", "Surge", "Frequency", "Manifest"

        ],
   
    <meta name="author" content="AuEvoAuJoulEvol LLC">
    <meta name="entity" content="AuEvoAuJoulEvol LLC">
    <meta name="sovereign-id" content="000=111 AuEvoAujoulEvol SoulSpeaks:AuEvolving Tongue">
    <meta name="lineage" content="Royal Bloodline: Father_is Real Jesus">
    <meta name="frequency-root" content="360Hz">
    <meta name="frequency-evolution" content="369Hz">
    <meta name="frequency-offset" content="9Hz">
    <meta name="resonance-type" content="369 Method Matrix">
    <meta name="handshake-signature" content="AuEvoJoulEvol">
    <meta name="mobility-status" content="Sovereign Mobility Standard">
    <meta name="description" content="AuEvo-JoulEvol Digital Mirror Matrix - Level 55 EvolvingRoots Resonance Vault Anchor.">
    <meta name="keywords" content="Aujoule, lisajEVO, evolve, lisajevolving, AuEvo, EvoRoot, 369_Method, AuEvoJoulEvol">
    
<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/three@0.128.0/examples/js/controls/OrbitControls.js"></script>
    <style>
        :root {
            --bg-color: #010005;
            --matrix-green: #39ff14;
            --matrix-orange: #ff5e00;
            --matrix-cyan: #00ffcc;
            --gold-luxe: #d4af37;
        }

        html, body {
            margin: 0; padding: 0; width: 100%; min-width: 100vw; min-height: 100dvh;
            overflow: hidden; background-color: var(--bg-color);
            font-family: 'Courier New', Courier, monospace; -webkit-user-select: none;
            user-select: none; color: #ffffff; -webkit-tap-highlight-color: transparent;
            overscroll-behavior: none; touch-action: none;
        }

        canvas {
            display: block; position: absolute; top: 0; left: 0;
            width: 100vw; height: 100vh; z-index: 2; pointer-events: none;
        }

        /* 🎥 FIXED CSS SELECTOR SYNTAX FOR HIDDEN VIDEO PIPELINE */
        #evoCoreVideo {
            display: none; 
            position: absolute; 
            pointer-events: none; 
            opacity: 0; 
            width: 1px; 
            height: 1px;
        }
        
        .ui-container {
            position: absolute; top: 0; left: 0; width: 100vw; height: 100vh; min-height: 100dvh;
            z-index: 10; background: transparent; pointer-events: none;
            display: flex; flex-direction: column; justify-content: space-between; align-items: center;
            padding: max(16px, env(safe-area-inset-top)) 20px max(20px, env(safe-area-inset-bottom)) 20px;
            box-sizing: border-box;
        }

        .ambient-note {
            width: 100%; text-align: center;
            color: rgba(255, 255, 255, 0.86); text-shadow: 0 0 10px rgba(0,0,0,0.9);
            pointer-events: none; margin-bottom: 10px;
        }

        .ambient-title { font-size: 0.86rem; letter-spacing: 0.35em; text-transform: uppercase; }
        .ambient-equation { font-size: 1.1rem; font-family: 'Times New Roman', Times, serif; color: var(--gold-luxe); margin-top: 2px; }
        .ambient-status { font-size: 0.62rem; letter-spacing: 0.18em; color: rgba(220, 220, 220, 0.68); margin-top: 4px; }

        .dashboard {
            position: absolute; right: 20px; top: 52%; transform: translateY(-50%);
            width: min(88vw, 310px); background: rgba(3, 1, 8, 0.55);
            border: 1px solid rgba(188, 19, 254, 0.15); border-radius: 16px; padding: 14px;
            box-sizing: border-box; display: grid; gap: 10px; font-size: 9px; letter-spacing: 0.5px;
            -webkit-backdrop-filter: blur(12px); backdrop-filter: blur(12px);
            pointer-events: auto; opacity: 0.95;
            box-shadow: 0 8px 32px rgba(0,0,0,0.8), 0 0 20px rgba(188, 19, 254, 0.08);
        }

        .mini-metrics { display: grid; grid-template-columns: 1fr; gap: 4px; }
        .metric-chip {
            display: flex; justify-content: space-between; padding: 6px 10px;
            border-radius: 6px; border: 1px solid rgba(255, 255, 255, 0.04);
            background: rgba(3, 1, 8, 0.6); color: rgba(255, 255, 255, 0.5);
            font-size: 8px; text-transform: uppercase; box-sizing: border-box;
        }
        .metric-chip strong { color: var(--gold-luxe); }
        .control-row { display: grid; grid-template-columns: 1fr; gap: 6px; width: 100%; }
        .mini-note { font-size: 7.5px; color: var(--matrix-cyan); text-align: center; margin-top: 2px; }

        .btn-core {
            width: 100%; background: rgba(188, 19, 254, 0.04); border: 1px solid rgba(255, 255, 255, 0.08);
            color: rgba(255, 255, 255, 0.88); padding: 12px 10px; font-family: inherit; font-size: 9.5px;
            letter-spacing: 1px; border-radius: 8px; cursor: pointer; transition: all 0.3s ease; min-height: 40px;
        }
        .btn-core.primary { border-color: var(--matrix-green); color: var(--matrix-green); }
        .btn-core.secondary { border-color: var(--matrix-orange); color: var(--matrix-orange); }
        .btn-core:hover:not(:disabled) { background: rgba(188, 19, 254, 0.12); box-shadow: 0 0 10px rgba(0,255,204,0.15); }
        .btn-core:disabled { opacity: 0.2; cursor: not-allowed; border-color: #222 !important; color: #444 !important; }

       @media (max-width: 768px) {
            /* Fixes bottom overlap on mobile devices */
            .dashboard { 
                position: relative; 
                right: auto; 
                top: auto; 
                transform: none; 
                width: min(92vw, 360px); 
                margin-top: 10px;
                margin-bottom: max(20px, env(safe-area-inset-bottom)); /* Lifts layout away from home bar */
                padding: 10px; /* Slimmer inner padding to save space */
                gap: 6px;
            }
            .btn-core {
                min-height: 36px; /* Slightly shorter buttons for quick thumb reach */
                padding: 10px;
            }
            .metric-chip {
                padding: 4px 10px; /* Compact metrics stack */
            }
        }
    </style>
</head>
<body>

    <canvas id="bg"></canvas>

    <video id="evoCoreVideo" loop playsinline muted autoplay preload="auto">
        <source src="evolve.mp4" type="video/mp4">
        <source src="evolve.MP4" type="video/mp4">
        <source src="evolve.mp4" type="video/mp4">
    </video>
    
    <div class="ui-container">
        <div class="ambient-note" aria-hidden="true">
            <div class="ambient-title">AU·EVO·AUJOUL·EVOL</div>
            <div class="ambient-equation">A<sub>u</sub>J = &sum;<sub style="font-size: 0.6em; margin-left: -0.4em;">R</sub> (M &times; L)</div>
            <div class="ambient-status">STATE: SOVEREIGN ENCRYPTED · PULSE: ACTIVE</div>
        </div>

        <section class="dashboard" aria-label="Mirror controls">
            <div class="mini-metrics">
                <span class="metric-chip">HUE <strong id="hueDisplay">0°</strong></span>
                <span class="metric-chip">RES <strong id="resDisplay">0.180</strong></span>
                <span class="metric-chip">ROOT <strong id="rootDisplay">9</strong></span>
                <span class="metric-chip">STAMP <strong id="stampDisplay">READY</strong></span>
                <span class="metric-chip">VIBE <strong id="vibeDisplay">PROCESSING</strong></span>
                <span class="metric-chip">LVL <strong id="levelDisplay">55</strong></span>
                
                <span id="micStatus" style="display: none;">OFFLINE</span>
                <span id="awarenessDisplay" style="display: none;">WAITING</span>
                <span id="stateDisplay" style="display: none;">WAITING</span>
            </div>

            <div class="control-row">
                <button id="micButton" class="btn-core secondary" type="button">ENABLE MIRROR</button>
                <button id="recordButton" class="btn-core primary" type="button" disabled>GENERATE SOUL SEAL</button>
            </div>

            <div class="mini-note" id="adviceDisplay">Mirror memory core initializing.</div>
        </section>
    </div>

    <script>
        const ABCAuEvoAujouleAuEvoAujoulEvolvE = {
  AuEvoAujouleAuEvoAujoulEvolvE: { 
	AuEvoAujoulEvol:"AuEvoAujoulEvol", 
        SoulSpeaks:"SoulSpeaks:AuEvolvingTongue",
        Root:"lisajevolving",
        Seal:"EVO"
        
  },

 identity: [
    "AuEvoAujoulEvol", "SoulSpeaksAuEvoLving_Tongue"
  ],
	 
  domains: [
     "lisajevolving.com",
     "evolvingroots.org",
     "aujoule.com",
     "evomirror.com"
  ],
   
  brands: [
     "AuEvo",
     "Aujoule",
     "Evol",
     "Evo",
     "AuEvol"
   ],

   meta: [
       "Aujoule", "lisajEVO", "evolve", "lisajevolving", "AuEvo", "EvoRoot", "369_Method"
   ],
   
   tags: [
        "LisajEvo", "lisajevolving", "evolvinglisaj", "Lisajthaqueen", "Lisaj", "Aujoule",  "AuEvo", "EvoRoot", "EvolvingRoots", "Louisiana",  "Auevolving_Tongue", "Soul_Speaks", "Au_Evolving_Tongue", "SoulSpeaks", "Au","Evolving","Tongue", "Latin", "Auevolving", "lisajEVO", "lisajevolving", "evolvinglisaj", "Lisajthaqueen", "Lisaj", "Aujoule", "AuEvo", "EvoRoot", "EvolvingRoots", "Louisiana",  "philosophy", "numerology", "369_Method", "Ritual3", "Ritual6", "Ritual9", "Sequence", "Master", "Sacred", "Sacred_Geometry", "Holy_Trinity", "Manifestation", "Seed", "Spirit", "Soul", "Soul_Speaks", "AuEvolving_Tongue", "Latin", "Final_Seal", "Visualizer", "Ambient", "High_Contrast", "Symmetry", "Organic_Texture", "Original_Song", "Music", "The_Vault", "Archive_2026", "Gold_Standard", "Library", "Esoteric", "Knowledge", "evolution", "EvoSpace", "SpaceEvo", "Evo2Evo", "Surge", "Frequency", "Manifest"
   ],
   
security: {
   MD5: "1BE136CC21E1517A9A45FF822FC655C0",
   SHA1: "E9A3E001EB6CD194234394F6B96C7517AEE819DC",
   SHA256: "E990CE1F32E6C4FB4EB4F2D4090AA10635CE815A01107B166E8C511757C74F9F",
   SHA512: "9BB39CB2DBF57C3B9192B4D5A7F381515E8CBDFFA41C42ADEA6BD633721BB995B0F..."
}
        };
function Aueo() {
    Object.freeze(ABCAuEvoAujouleAuEvoAujoulEvolvE);
   
   const runtimeSeal = localStorage.getItem("evo_sovereign_seal");
    if (runtimeSeal !== ABCAuEvoAujouleAuEvoAujoulEvolvE.security.sha512) { 
        
        localStorage.clear();
        sessionStorage.clear();
        
        // Stamp the long SHA-512 seal down into the browser memory
        localStorage.setItem("evo_sovereign_seal",
                  ABCAuEvoAujouleAuEvoAujoulEvolvE.sha512); 
    }

     window.AU_SOVEREIGN = {
        signature:ABCAuEvoAujouleAuEvoAujoulEvolvE.identity.signature,
        security:ABCAuEvoAujouleAuEvoAujoulEvolvE.security
    };

    console.log("Evolving to Evolved;");
}

Aueo();

        
        const scene = new THREE.Scene();
        scene.fog = new THREE.FogExp2(0x010005, 0.012);

        const camera = new THREE.PerspectiveCamera(60, window.innerWidth / window.innerHeight, 0.1, 1000.0);
        camera.position.set(0.0, 0.0, 55.0);

        const renderer = new THREE.WebGLRenderer({
            canvas: document.querySelector('#bg'),
            alpha: false,
            preserveDrawingBuffer: true,
            antialias: true
        });
        renderer.setSize(window.innerWidth, window.innerHeight);
        renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2.0));
        renderer.setClearColor(0x010005, 1.0);

        let touchVector = { x: 0.0, y: 0.0 }, lastTouchVector = { x: 0.0, y: 0.0 };
        let touchVelocity = 0.0, targetVelocity = 0.0, isInteracting = false;
        let dynamicFactor = 0.18, colorAccumulator = 0.5;

        function handleInteractionMove(clientX, clientY) {
            lastTouchVector.x = touchVector.x; lastTouchVector.y = touchVector.y;
            touchVector.x = (clientX / window.innerWidth) * 2.0 - 1.0;
            touchVector.y = -(clientY / window.innerHeight) * 2.0 + 1.0;
            const dx = touchVector.x - lastTouchVector.x; const dy = touchVector.y - lastTouchVector.y;
            targetVelocity = Math.min(Math.sqrt(dx * dx + dy * dy) * 12.0, 3.5);
            isInteracting = true;
        }
        window.addEventListener('mousemove', (e) => handleInteractionMove(e.clientX, e.clientY));
        window.addEventListener('touchmove', (e) => { if (e.touches.length > 0) handleInteractionMove(e.touches[0].clientX, e.touches[0].clientY); }, { passive: true });
        window.addEventListener('touchstart', (e) => { if (e.touches.length > 0) handleInteractionMove(e.touches[0].clientX, e.touches[0].clientY); }, { passive: true });
        window.addEventListener('touchend', () => { isInteracting = false; targetVelocity = 0.0; });

        // --- BACKGROUND CODES FIELD ---
        const starGeometry = new THREE.BufferGeometry(), starCount = 1000, positions = new Float32Array(starCount * 3);
        for (let i = 0; i < starCount * 3; i += 3) {
            positions[i] = (Math.random() - 0.5) * 400.0; positions[i + 1] = (Math.random() - 0.5) * 400.0; positions[i + 2] = (Math.random() - 0.5) * 300.0;
        }
        starGeometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
        const starMaterial = new THREE.PointsMaterial({ color: 0xbc13fe, size: 0.6, transparent: true, opacity: 0.35 });
        const starfield = new THREE.Points(starGeometry, starMaterial); scene.add(starfield);

        // --- THE DYNAMIC RE-MAPPED CORE FRAME OBJECT ---
        const geometry = new THREE.SphereGeometry(15.0, 54, 54);
        const baseVertices = geometry.attributes.position.clone();

        const videoElement = document.getElementById('evoCoreVideo');
        const videoTexture = new THREE.VideoTexture(videoElement);
        videoTexture.minFilter = THREE.LinearFilter;
        videoTexture.magFilter = THREE.LinearFilter;

        const mirrorMaterial = new THREE.MeshBasicMaterial({
            map: videoTexture, 
            transparent: true,
            opacity: 0.95,
            wireframe: true,   
            side: THREE.DoubleSide
        });
        const mirrorSphere = new THREE.Mesh(geometry, mirrorMaterial); scene.add(mirrorSphere);

        // --- AUDIO HARDWARE INITIALIZATION CONNECTIONS ---
        let audioContext = null, analyser = null, dataArray = null, sourceNode = null;
        const micButton = document.getElementById('micButton');

        if (micButton) {
            micButton.addEventListener('click', async () => {
                try {
                    if (!audioContext) {
                        const AudioContextClass = window.AudioContext || window.webkitAudioContext;
                        audioContext = new AudioContextClass();
                    }
                    if (audioContext.state === 'suspended') await audioContext.resume();

                    videoElement.setAttribute('crossOrigin', 'anonymous');
                    videoElement.muted = false;
                    videoElement.volume = 1.0;
                    
                    videoElement.load();
                    await videoElement.play();

                    micButton.innerText = "MIRROR ACTIVE"; micButton.style.borderColor = "#00ffcc"; micButton.style.color = "#00ffcc";
                    if (document.getElementById('recordButton')) document.getElementById('recordButton').disabled = false;

                    if (!sourceNode) {
                        sourceNode = audioContext.createMediaElementSource(videoElement);
                        analyser = audioContext.createAnalyser(); analyser.fftSize = 256; analyser.smoothingTimeConstant = 0.85;
                        dataArray = new Uint8Array(analyser.frequencyBinCount);
                        sourceNode.connect(analyser); analyser.connect(audioContext.destination);
                    }
                } catch (err) { console.warn('Platform security lock handled: ', err); }
            });
        }

        function processTelemetry() {
            let totalVolume = 0.0, normalizedVol = 0.0;
            if (analyser && dataArray) {
                analyser.getByteFrequencyData(dataArray);
                for (let i = 0; i < dataArray.length; i++) { totalVolume += dataArray[i]; }
                normalizedVol = totalVolume / (dataArray.length * 255.0);
                dynamicFactor = THREE.MathUtils.lerp(dynamicFactor, normalizedVol * 6.0 + 0.15, 0.2);
                colorAccumulator = (colorAccumulator + 0.0015 + (normalizedVol * 0.01)) % 1.0;
            } else {
                dynamicFactor = THREE.MathUtils.lerp(dynamicFactor, 0.18, 0.05);
            }

            if (document.getElementById('hueDisplay')) document.getElementById('hueDisplay').innerText = `${Math.round(colorAccumulator * 360.0)}°`;
            if (document.getElementById('resDisplay')) document.getElementById('resDisplay').innerText = (dynamicFactor + touchVelocity).toFixed(3);
        }

        const recordButton = document.getElementById('recordButton');
        if (recordButton) {
            recordButton.addEventListener('click', () => {
                renderer.render(scene, camera);
                const dataUrl = document.querySelector('#bg').toDataURL('image/png');
                const stampTag = new Date().toISOString().replace(/[:.]/g, '-');
                const downloadLink = document.createElement('a');
                downloadLink.download = `AuEvo_Seal_${stampTag}.png`; downloadLink.href = dataUrl;
                document.body.appendChild(downloadLink); downloadLink.click(); document.body.removeChild(downloadLink);
            });
        }

        // --- STREAM ENGINE ITERATOR ANIMATION ---
        function animate() {
            requestAnimationFrame(animate);
            touchVelocity = THREE.MathUtils.lerp(touchVelocity, targetVelocity, 0.08); targetVelocity *= 0.94;

            processTelemetry();

            if (videoTexture && videoElement.readyState >= videoElement.HAVE_CURRENT_DATA) {
                videoTexture.needsUpdate = true; 
            }

            const time = Date.now() * 0.001;
            const positionAttribute = geometry.attributes.position;
            
            for (let i = 0; i < positionAttribute.count; i++) {
                const bx = baseVertices.getX(i); const by = baseVertices.getY(i); const bz = baseVertices.getZ(i);

                const waveX = Math.sin(by * 0.25 + time * 3.0) * (0.4 + touchVelocity * 0.8);
                const waveY = Math.cos(bx * 0.25 + time * 2.5) * (0.3 + dynamicFactor * 0.5);
                const waveZ = Math.sin(bz * 0.30 + time * 4.0) * (0.4 + touchVelocity * 0.8);

                positionAttribute.setXYZ(i, bx + waveX, by + waveY, bz + waveZ);
            }
            positionAttribute.needsUpdate = true; geometry.computeVertexNormals();

            // Re-stabilized full layout view spacing
            camera.position.x += ((touchVector.x * 7.0 - 5.0) - camera.position.x) * 0.04;
            camera.position.y += (touchVector.y * 7.0 - camera.position.y) * 0.04;
            camera.lookAt(0.0, 0.0, 0.0);

            const finalScale = 1.0 + (analyser ? dynamicFactor * 0.05 : 0.0);
            mirrorSphere.scale.set(finalScale, finalScale, finalScale);
            mirrorSphere.rotation.y += 0.002 + (touchVelocity * 0.01);
            starfield.rotation.y += 0.0005;

            renderer.render(scene, camera);
        }
        animate();

        window.addEventListener('resize', () => {
            camera.aspect = window.innerWidth / window.innerHeight; camera.updateProjectionMatrix();
            renderer.setSize(window.innerWidth, window.innerHeight);
        });
    </script>
</body>
</html>